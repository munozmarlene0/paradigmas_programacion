#include <stdio.h>                              // Librería para entrada/salida

int main() {                                    // Función principal del programa
    
    for (int i = 1; i <= 5; i++) {              // Desde i=1 hasta i=5, incrementar i
        printf("%d\n", i);                      // Imprimir valor de i
    }
    
    return 0;                                   // Fin del programa
}
